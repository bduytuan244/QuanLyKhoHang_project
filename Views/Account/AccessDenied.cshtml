@{
    ViewData["Title"] = "Access Denied";
}

<style>
    .access-denied-container {
        min-height: 100vh;
        background: linear-gradient(135deg, #1e3c72 0%, #2a5298 50%, #7e8ba3 100%);
        position: relative;
        overflow: hidden;
        display: flex;
        align-items: center;
        padding: 50px 0;
    }

        .access-denied-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg width="100" height="100" xmlns="http://www.w3.org/2000/svg"><rect width="2" height="2" fill="white" opacity="0.1"/></svg>') repeat;
            opacity: 0.3;
        }

    .floating-shape {
        position: absolute;
        border-radius: 50%;
        background: rgba(255,255,255,0.1);
        animation: floatShape 6s ease-in-out infinite;
    }

    .shape-1 {
        width: 100px;
        height: 100px;
        top: 10%;
        left: 10%;
        animation-delay: 0s;
    }

    .shape-2 {
        width: 150px;
        height: 150px;
        bottom: 10%;
        right: 10%;
        animation-delay: 2s;
    }

    .shape-3 {
        width: 80px;
        height: 80px;
        top: 50%;
        right: 5%;
        animation-delay: 4s;
    }

    @@keyframes floatShape {
        0%, 100% {
            transform: translateY(0px) scale(1);
        }

        50% {
            transform: translateY(-20px) scale(1.1);
        }
    }

    .error-card {
        background: white;
        border-radius: 20px;
        box-shadow: 0 20px 60px rgba(0,0,0,0.3);
        overflow: hidden;
        position: relative;
        z-index: 1;
    }

    .error-icon-wrapper {
        background: linear-gradient(135deg, #e74c3c 0%, #c0392b 100%);
        width: 150px;
        height: 150px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 30px;
        box-shadow: 0 15px 40px rgba(231, 76, 60, 0.4);
        animation: pulse 2s ease-in-out infinite;
    }

    @@keyframes pulse {
        0%, 100% {
            transform: scale(1);
        }

        50% {
            transform: scale(1.05);
        }
    }

    .error-icon-wrapper i {
        font-size: 5rem;
        color: white;
    }

    .error-code {
        font-size: 5rem;
        font-weight: 700;
        background: linear-gradient(135deg, #e74c3c 0%, #c0392b 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        margin-bottom: 20px;
    }

    .error-title {
        color: #1e3c72;
        font-weight: 700;
        font-size: 2.5rem;
        margin-bottom: 20px;
    }

    .error-description {
        color: #5a6c7d;
        font-size: 1.2rem;
        line-height: 1.8;
        margin-bottom: 40px;
        max-width: 500px;
        margin-left: auto;
        margin-right: auto;
    }

    .btn-custom-primary {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        border: none;
        padding: 15px 35px;
        font-size: 1.1rem;
        font-weight: 600;
        border-radius: 50px;
        color: white;
        transition: all 0.3s ease;
        box-shadow: 0 8px 20px rgba(102, 126, 234, 0.3);
        margin: 10px;
    }

        .btn-custom-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 30px rgba(102, 126, 234, 0.4);
            background: linear-gradient(135deg, #764ba2 0%, #667eea 100%);
            color: white;
        }

    .btn-custom-outline {
        background: transparent;
        border: 2px solid #667eea;
        padding: 15px 35px;
        font-size: 1.1rem;
        font-weight: 600;
        border-radius: 50px;
        color: #667eea;
        transition: all 0.3s ease;
        margin: 10px;
    }

        .btn-custom-outline:hover {
            background: #667eea;
            color: white;
            transform: translateY(-2px);
        }

    .help-box {
        background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
        padding: 20px 30px;
        border-radius: 15px;
        margin-top: 40px;
        display: inline-block;
    }

        .help-box p {
            color: #1e3c72;
            font-weight: 600;
            font-size: 1.05rem;
            margin: 0;
        }

        .help-box i {
            color: #667eea;
            font-size: 1.3rem;
            margin-right: 10px;
        }
</style>

<div class="access-denied-container">
    <!-- Floating Shapes -->
    <div class="floating-shape shape-1"></div>
    <div class="floating-shape shape-2"></div>
    <div class="floating-shape shape-3"></div>

    <div class="container">
        <div class="row justify-content-center">
            <div class="col-xl-8 col-lg-9 col-md-10">
                <div class="error-card">
                    <div class="card-body p-5 text-center">
                        <div class="error-icon-wrapper">
                            <i class="fas fa-shield-alt"></i>
                        </div>

                        <h1 class="error-code">403</h1>
                        <h2 class="error-title">Truy Cập Bị Từ Chối</h2>
                        <p class="error-description">
                            Bạn không có quyền truy cập vào trang này.
                            Vui lòng liên hệ với quản trị viên hệ thống nếu bạn cho rằng đây là một lỗi.
                        </p>

                        <div class="mt-4">
                            <a class="btn btn-custom-primary" asp-controller="Dashboard" asp-area="Admin" asp-action="Index">
                                <i class="fas fa-home mr-2"></i>Về Trang Chủ
                            </a>
                            <a class="btn btn-custom-outline" href="javascript:history.back()">
                                <i class="fas fa-arrow-left mr-2"></i>Quay Lại
                            </a>
                        </div>

                        <div class="help-box">
                            <p>
                                <i class="fas fa-headset"></i>
                                Cần hỗ trợ? Liên hệ quản trị viên:
                                <a href="mailto:admin@dthlogistics.com" style="color: #667eea; text-decoration: none; font-weight: 700;">admin@dthlogistics.com</a>
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
